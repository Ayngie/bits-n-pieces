import AddNumbers from './Inputs/AddNumbers';

const Inputs = () => {
  return (
    <section className="card">
      <h2>Forms:</h2>

      <AddNumbers />
    </section>
  );
};

export default Inputs;
