import Counter from './Buttons/Counter';

const Buttons = () => {
  return (
    <section className="card bordered-section">
      <h2>Buttons:</h2>

      <Counter />
    </section>
  );
};

export default Buttons;
