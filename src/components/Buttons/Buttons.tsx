import Counter from './Counter';
import Toggles from './Toggles';

const Buttons = () => {
  return (
    <section className="card">
      <h2>Buttons:</h2>
      <Counter />
      <Toggles />
    </section>
  );
};

export default Buttons;
